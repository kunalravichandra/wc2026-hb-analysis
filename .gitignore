# Virtual environment — never upload this (it's huge and machine-specific)
venv/

# API keys — CRITICAL: never upload this to GitHub
.env

# Raw data — too large for GitHub, regenerated by scripts
data/raw/

# Python cache files — auto-generated, not needed
__pycache__/
*.pyc
*.pyo

# VS Code local settings
.vscode/settings.json

# Jupyter notebook checkpoints
.ipynb_checkpoints/

# Log files
logs/

